# -*- mode: snippet -*-
# name: BaseView
# key:  bv
# --
var BaseView = require('BaseView');

return BaseView.extend({
  template: '$1',

  events: {
  },

  initialize: function(options) {
    $0
  }
});